Closes ICE-189

## Summary
Implements the platform summary endpoint (restricted to master keys) to calculate platform reconciliation rates, and the transaction detail endpoint (vendor-scoped) for retrieving single transactions.

## Scope
- `src/controllers/statements.controller.ts`: Appends `getPlatformSummary` and `getTransactionById` routes. Added `AppError` and validation for required IDs.
- `src/repositories/statements.repo.ts`: Extended with aggregation methods.
- `tests/integration/summary.test.ts`: Added full `vitest` suite for controller logic, testing both master key and vendor-scoped auth states.
- Rebased target branch to `dev`.

## Test Plan
- Integration tests cover auth context extraction and correct service method invocation.

## Risk
Low. Reads only. Auth contexts safely passed to the service layer.
