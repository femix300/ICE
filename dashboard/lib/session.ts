// The ICE API is key-authenticated; the active identities are resolved from the
// authenticated session. Until the session layer lands, these are supplied here
// as a single source of truth so every dashboard stays scoped to one owner/vendor.
export const CURRENT_MERCHANT_ID = '00000000-0000-0000-0000-000000000000';
export const CURRENT_VENDOR_ID = '11111111-1111-1111-1111-111111111111';
