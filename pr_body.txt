Closes ICE-304

## Summary
Implements the auto-refund engine for handling overpayments.

## Scope
- `src/workers/refund.worker.ts`: Executes Nomba account lookup and transfer API. Uses shared `AppError` and proper ESM imports.
- `src/repositories/refunds.repo.ts`: Manages refund statuses.
- `tests/unit/refund.worker.test.ts`: Covers kobo processing, lookup-before-transfer, and failure states.

## Test Plan
- Verified `npm test` passes for the refund worker logic.
- Confirmed no kobo-to-naira conversion happens internally.

## Risk
Medium. Uses Nomba transfer API. Mitigated by `lookupBank` verification step and strict Zod payload validation.
